import 'package:flutter/material.dart';

class MainScreen2 extends StatelessWidget {
  final BuildContext context;
  const MainScreen2({Key? key, required this.context}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('0'),
    );
  }
}
